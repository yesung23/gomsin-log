node_modules
dist
.codex-runtime/
*.local
.env
.env.local
Microsoft/
.env.*.local


# Capacitor native project.
# Generated by `npm run cap:add:android`; regenerate rather than commit it.
# Anything hand-edited there (the deep-link intent-filter, signing config) is
# documented in docs/kiro/PLAY_STORE_ROADMAP.md so it can be reapplied.
/android
/ios

# Android signing material -- must never be committed.
*.keystore
*.jks
keystore.properties

# TypeScript incremental build cache (build artifact)
*.tsbuildinfo

# Scratch output from review tooling. Conclusions that matter are written into
# docs/kiro/RELEASE_AUDIT_2026-07-31.md instead.
/semantic-review
