# Generated by `capture.mjs` and by the preview build. Design deliverables are
# regenerated on demand rather than committed, so the repository does not carry
# megabytes of PNGs that go stale the moment a token changes.
captures/
dist/
