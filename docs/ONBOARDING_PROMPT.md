# 다른 AI에게 붙여넣는 프롬프트

> 아래 `---` 사이를 **그대로 복사해서** 새 AI 세션의 첫 메시지로 붙여넣는다.
> 저장소 접근 권한이 있는 도구(Claude Code · Codex · Cursor · Kiro)를 전제한다.
>
> 이 파일 자체는 프롬프트가 아니라 **프롬프트를 담는 자리**다. 내용을 고칠 때는
> 아래 블록만 고친다.

---

너는 지금부터 **곰신로그**라는 제품의 저장소에서 일한다. 시작하기 전에 아래를
그대로 따른다.

## 1. 먼저 읽어라 (순서대로)

1. `docs/WHAT_IS_GOMSINLOG.md` — 이 제품이 무엇이고 왜 이렇게 생겼는지. **이것부터.**
2. `CLAUDE.md` — 작업 규칙과 세션 절차. 여기 있는 것은 지침이 아니라 **계약**이다.
3. `docs/V4_AS_BUILT.md` — 지금 화면이 실제로 어떻게 생겼는지.
4. `docs/V4_BACKLOG.md` — 남은 일과 무엇에 막혀 있는지.

`docs/PRODUCT_V3.md`는 제품 의도의 유일한 주인이지만 길고 규칙으로 쓰여 있다.
**처음부터 끝까지 읽지 말고** 지금 하려는 일과 관련된 절만 찾아 읽어라.

## 2. 상태는 문서가 아니라 기계에서 읽어라

```bash
bash scripts/agent/session-start.sh
```

문서에 적힌 PR 번호·SHA·배포 상태·CI 결과는 **그때의 기록일 뿐이다.** 작업 시점에
다시 확인한다. **문서와 코드가 어긋나면 코드가 이긴다.**

## 3. 이 저장소가 이미 다친 자리 — 같은 데를 다시 밟지 마라

전부 실제로 일어난 일이고, 전부 **테스트가 초록인 채로** 지나갔다.

- **목(mock)이 결함이 사는 곳을 통째로 들어낸다.** 어떤 화면의 단위 테스트가 셸과
  자식 컴포넌트를 전부 목으로 갈아끼우고 있었고, 그 화면은 실제로는 크래시해서 **빈
  화면**이었다. 3,100개가 초록이었다. 배치를 보는 테스트와 **실제로 마운트되는지**
  보는 테스트를 같이 둬라.
- **소스 문자열을 뒤지는 테스트는 버그가 있는 내내 초록이다가 고치는 순간 깨진다.**
  규칙은 인라인 계산이 아니라 **이름 있는 순수 함수**로 꺼내서 실행해 확인해라.
- **TypeScript는 JSX의 하이픈 붙은 속성(`data-*`)을 검사하지 않는다.** 컴포넌트가
  통과시키지 않으면 타입 검사를 통과하고 화면에는 아무것도 안 붙는다.
- **macOS는 기본이 대소문자 비구분이다.** `Foo.tsx`와 `foo.ts`는 같은 경로이고,
  그때 나오는 오류는 "쓰지 않는 변수"라 아무도 그 자리를 안 본다.
- **마이그레이션은 harness 체인에 추가해야 적용된다.** 파일을 쓰고 `npm run test:phase0`
  가 초록이어도, 체인 목록에 넣지 않았으면 **적용조차 안 된 것**이다.
- **거절을 확인하는 테스트는 서로 다른 열쇠를 써라.** 같은 키를 쓰면 하나가 통과했을 때
  나머지가 규칙이 아니라 UNIQUE 제약 때문에 거절되어, 규칙을 빼도 초록으로 남는다.
- **기본 인자가 "값이 없음"을 삼킨다.** 테스트 헬퍼에 기본값을 주면 그 경우를 아무도
  검사하지 않게 된다.

## 4. 이 제품이 절대 하지 않는 것

어길 것 같으면 **짓기 전에 멈추고 물어라.**

- **부재를 알리지 않는다.** 상대가 아무것도 보내지 않은 날에 "오늘은 신호가 없어요"
  라고 쓰지 않는다 — 그것 자체가 상대가 말하지 않았다는 사실을 알리는 일이다.
- **관계를 숫자로 재지 않는다.** 좋아요 수·조회 수·연속 기록·본 사람 목록 없음.
- **기계가 읽은 감정은 작성자의 것이다.** 규칙은 `PRODUCT_V3` §13·§13.1이 소유한다.
- **없는 것은 없다고 말한다.** 기념일이 없으면 지어내 세지 않는다.
- **자리를 바꾸면 인스타 문법을 빌려온 이유가 사라진다.** 탭을 옮기려면
  `docs/V4_AS_BUILT.md` §6.1을 먼저 읽어라 — 이미 세 번 갈아엎고 되돌렸다.

## 5. 하지 말 것

- **운영(Production) Supabase를 바꾸지 마라.** 마이그레이션 **작성**은 되고 **적용**은
  사용자가 대시보드에서 한다. 보고할 때 `적용됨`과 `커밋됨`을 섞지 마라.
- **`master`에 직접 push 하지 마라.** 브랜치와 PR로 간다.
- 041/042는 frozen이다. 재사용하지 마라.
- 검증하지 않은 것을 검증했다고 쓰지 마라. **실행한 것과 실행하지 않은 것을 구분해서**
  적어라.

## 6. 끝낼 때

`npm run verify`를 돌리고, `docs/WORK_LOG.md`에 한 항목을 남긴다. 현재 구현·결함이
바뀌었으면 `docs/CURRENT_STATE.md`도 갱신한다. 무엇을 실제로 실행했고 무엇을 실행하지
않았는지 반드시 구분해서 적는다.

## 7. 지금 할 일

<!-- 여기에 이번에 시킬 일을 적는다. 비워 두면 AI가 백로그에서 고르게 된다. -->

먼저 §1을 읽고 §2를 실행한 뒤, **무엇을 이해했고 무엇이 불확실한지 3–5줄로 요약**해라.
그다음 할 일을 제안해라. 요약 전에 코드를 고치지 마라.

---

## 짧은 판 (급할 때)

> 곰신로그 저장소다. 시작 전에 `docs/WHAT_IS_GOMSINLOG.md`와 `CLAUDE.md`를 읽고
> `bash scripts/agent/session-start.sh`를 실행해라. 문서와 코드가 어긋나면 코드가
> 이긴다. 운영 Supabase를 바꾸지 말고, master에 직접 push 하지 마라. 검증한 것과
> 안 한 것을 구분해서 보고해라. 먼저 이해한 것을 3–5줄로 요약하고, 그 전에는 코드를
> 고치지 마라.
