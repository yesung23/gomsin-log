# V4 백로그 — 요청받은 것과, 계획했는데 안 지은 것

> 2026-08-23 작성. **지어진 것의 지도는 [`V4_AS_BUILT.md`](V4_AS_BUILT.md)**,
> 계획의 원본은 [`EXPERIENCE_V4_MASTER_PLAN.md`](EXPERIENCE_V4_MASTER_PLAN.md)다.
> 이 문서는 **그 둘의 차이**만 담는다 — 무엇이 남았고, 무엇에 막혀 있고, 왜 그 순서인지.
>
> 각 항목의 상태는 코드에서 직접 확인했다. `NOT BUILT`는 "찾아봤는데 없다"이지
> "아마 없을 것"이 아니다.

---

## A. 이번에 새로 요청받은 것 (2026-08-23)

### A1. 탭 `+` = 스토리와 게시물을 조합한 **나만의 일기장**

지금 `+`는 컴포저(`/compose`)다. 요청은 **엮는 화면**이다 — 그날의 스토리와 게시물을
골라 한 지면으로 만드는 것.

`일기장`(`/diary`)이 이미 월 단위로 그 일을 하지만 **자동으로 엮기만** 하고 고르지
못한다. 둘을 합쳐야 한다: 자동으로 엮어 오되 무엇을 넣고 뺄지는 사용자가 정한다.

- 남는 질문: 컴포저는 어디로 가는가. 홈 레일의 `+`와 `우리`·`찾기`의 펜이 이미 §7.1을
  지고 있으므로 탭에서 빠져도 계약은 깨지지 않는다
- 영향: `PRODUCT_V3` §5 탭 표 개정 필요
- 상태: **NOT BUILT**

### A2. `우리` 탭의 격자 = 캘린더가 아니라 **게시물**

> **요청 원문:** "마이탭에는 캘린더가 아니라 게시물(본인이 원하는 사진과 글을 쓸 수 있게
> 함, 인스타그램의 게시물과 동일)하게 만들어"

지금 `우리`의 첫 탭은 **여행 기간 안의 사진 첨부 기록만** 인스타 게시물처럼 보여주는
3열 격자다. 사진이 없는 글·영상·음성 전용 기록은 이 표면에 나오지 않는다. 사진 타일을
누르면 사진이 중심인 상세 viewer가 열리고, 여러 장 넘기기·확대는 기존 미디어 갤러리를
그대로 쓴다. 글은 사진 아래의 보조 캡션으로만 보인다. 일반 기록 전체는 `사진` 탭의
기존 기록 목록에서 읽는다.

- 계획 #26이 하루 격자를 고른 이유는 사진을 잘 안 올리는 커플에게 **구멍 난 격자**가
  되는 것을 막기 위해서였다. 이번에는 여행 기록만 게시물 격자로 제한하고 일반 기록은
  사진 탭에서 보이게 하여 그 우려를 줄였다
- `DailyRecord`에 `trip_id`가 없어 기존 여행/여행 일정 이벤트의 날짜 범위로 판별한다.
  따라서 같은 날의 일반 기록이 여행 게시물로 잡힐 수 있다는 한계를 문서에 남겼다
- `사진` 탭은 기존의 기록 중심 목록(내용·미디어·상세·타임라인)을 담당하고, `여행` 탭은
  현재·예정·지난 여행을 최대 세 개까지 간단히 보여준 뒤 카드 상세와 `/trips` 전체 보기로
  이어진다
- 상태: **DONE** (2026-08-23, 사진 전용 게시물/상세 viewer로 보정)

### A3. `찾기` 탭에 역할별 정보

> **요청 원문:** "찾기 탭에는 군화는 군 관련 정보들을 넣고, 곰신은 생리주기 체크 및
> 상대방에게 공유 기능을 넣어줘"

그 내용은 이미 `/me`(오늘 내 상태)에 다 있다 — 복무 현황·연락 가능 시간·주기·배려
신호. 지금은 `우리`의 칩으로만 닿는다. 요청은 그것을 **`찾기` 탭으로** 올리는 것.

- 검색어가 없을 때 군화는 복무·연락 가능 시간·다음 휴가/면회를, 곰신은 기존 주기 표면을
  먼저 본다. 검색어를 입력하면 기존 날짜/내용 검색 결과만 보여준다
- 상태: **DONE** (2026-08-23)

### A4. 일정 — 여러 개를 한 번에

지금은 **연속 범위**만 된다(끌어서 `startDate`~`endDate`). 요청은 **여러 개**를 골라
한 번에 적는 것 — 흩어진 날에 각각 일정을 만드는 것.

- 프리뷰(`InstaPlan`)에는 흩어진 선택이 있었고 앱에는 범위만 이식했다
- 데이터: 흩어진 날 N개는 이벤트 N건이다. 같은 제목·종류로 한 번에 만들면 된다
- 상태: **DONE** (2026-08-23). 끌면 구간이 더해지고 톡 누르면 그 하루가 켜지고 꺼진다.
  저장할 때 `groupIntoRuns` 가 연속 구간으로 잘라 **구간 하나가 일정 하나**가 된다 --
  붙은 날은 기간 하나로, 흩어진 날은 각각 하루로. 일부만 성공하면 몇 건이 되었는지
  그대로 말한다

### A5. 일정 — 시간 입력이 깨지던 것 ✅ DONE

`우리 할 일 빠르게 추가`가 `flex-1`로 남는 폭을 다 먹어 시간 입력이 80px에 갇혔다.
`<input type="time">`은 브라우저가 그리는 컨트롤이라 `--:--`와 시계 아이콘이 잘렸다.

두 줄로 나눴다 — 제목이 한 줄, 시간과 버튼이 아래 줄. 시간에 최소 폭 `7.5rem`과
`tabular-nums`를 줘서 값이 바뀔 때 줄이 흔들리지 않는다. 시간을 **지우는 길**도 넣었다.

- 상태: **DONE** (2026-08-23)

### A6. 여행 — 사진 여러 장 넣으면 자동 입력, 순서 변경

- **순서 변경은 있다.** `reorderTripItemsInDB`가 있고 `TripDetailPage`가 쓴다. 다만
  위/아래 버튼 방식이라 "쉽게"는 아니다 — 끌어 옮기기가 아니다
- **사진에서 자동 입력은 없다.** 지도 스크린샷 하나를 항목으로 만드는 경로가 있을 뿐,
  여러 장에서 정보를 뽑는 것은 **NOT BUILT**
- 남는 질문: "자동으로 정보가 입력"이 무엇을 뜻하는가 — EXIF 위치·시각인지, 사진 속
  글자(OCR)인지, 순서만인지. **EXIF는 §19가 허용하지 않는 종류의 데이터**라 먼저 판단이
  필요하다
- 상태: **순서 변경 DONE** (2026-08-23, 손잡이를 끌어 옮긴다. 화살표는 키보드·스크린리더를
  위해 남겼다) / **자동 입력 NOT BUILT** — 무엇을 뽑을지 정해지기 전에는 짓지 않는다

---

## B. 계획서에 있는데 안 지어진 것

계획서 §7.1 배치표를 코드와 하나씩 대조했다.

| # | 계획한 것 | 계획한 자리 | 지금 |
|---|---|---|---|
| 9 | **요약을 홈 지면에** | `홈/지면/편지` | 스토리 첫 장으로 **이동함** (의도된 변경, AS_BUILT §6.2) |
| 11 | **반응 — 공감·토닥이기 도장** | `스토리/액션` · `홈/지면/카드` | **NOT BUILT.** 보는 사람의 반응이 데이터 모델에 없다 |
| 13 | 이야기거리 보관함 | `홈/상단바/우1` | **DONE** (`/saved`) |
| 14 | 통화 모드 | `홈/상단바/우2` | **DONE** (`/call`). 단 "0개면 숨김"은 미이행 |
| 16 | **사진 아카이브** | `찾기/격자` · `우리/탭2` | **`우리/탭2`는 기존 기록 목록으로 DONE** (2026-08-23). 사진 탭은 게시물 격자를 복제하지 않고 상세·타임라인으로 이어진다 |
| 22 | 복무율 | `일정/하단` | **DONE** |
| 25 | 여행 플래너 | `우리/탭3` | **DONE** (2026-08-23). `우리/탭3`가 여행 요약을 최대 세 개 보여주고 카드 상세·`/trips` 전체 보기로 이어진다 |
| 27 | 이 달 다시 보기 | `우리/격자` 하단 | **DONE** (`더 보기`) |
| 29 | **1년 전 오늘** | `홈/지면` 위 조용한 한 줄 | **DONE** (2026-08-23) |
| 30 | **배려 신호를 홈 레일에** | `홈/레일/쪽지` | **DONE** (2026-08-23) |
| 32 | 파트너 주기 projection | 상대 `홈/레일/쪽지` 근처 | **NOT BUILT** |
| 41 | 위젯 커스터마이즈 강등 | `설정/홈 구성` | **절반.** 홈에서 뺐으나 설정에 자리를 안 만들었다 |

### B1. 특히 눈에 띄는 둘

**배려 신호(#30)가 홈에 없다.** 계획서가 "인스타의 노트(Notes)" 자리라고 못 박았고,
그게 이 기능이 매일 보이는 유일한 길이었다. 지금은 `우리 → 오늘 내 상태`를 두 번
들어가야 나온다 — **묻혀 있던 곳에서 조금 덜 묻힌 곳으로 옮겼을 뿐**이다.

**1년 전 오늘(#29)이 없다.** 축적이 이 제품의 north star인데, 쌓인 것이 스스로 돌아오는
유일한 장치가 이것이었다.

---

## C. 게이트에 막힌 것 (코드로 못 여는 것)

| 무엇 | 막는 것 | 누가 여는가 |
|---|---|---|
| 공감·토닥이기 | 보는 사람 반응 테이블·RLS | migration gate |
| 일기장 스티커 동기화 | CSK 도메인 테이블·RLS | migration gate |
| `한 권으로 만들기` 결제 | `P-MP` 게이트 셋 | product/engineering gate |
| 유료 스티커 | §9.5 — Memory Product 검증 뒤 | BUSINESS 문서 |
| 주기 트래커 역할 해제 | HRK 도메인, §21 재검토 | security review |
| 외박·외출 일정 종류 | CHECK 제약 변경 | migration gate |
| 여행 사진 EXIF 자동 입력 | §19가 허용하는 데이터 범위 | product 판단 |

---

## D. 순서 제안

> 2026-08-23: **하나 · 둘 · 넷 · 다섯(순서 변경) · A2 · A3를 지었다.** 아래는 남은 것이다.

**셋: A1 탭 재편** — `+`를 일기장으로, 컴포저를 어디로, `찾기`를 무엇으로. 이건
`PRODUCT_V3` §5 개정이 먼저다. **탭 자리를 네 번째로 바꾸는 것이므로**
[`V4_AS_BUILT.md`](V4_AS_BUILT.md) §6.1을 먼저 읽고 같은 이유로 되돌리지 않을지
확인한다.

**다섯: A6 여행 자동 입력** — EXIF인지 OCR인지 순서만인지 정해지기 전에는 짓지 않는다.
§19가 허용하는 데이터 범위가 걸려 있다.

---

## F. 최적화 — gpt-5.6-sol 전수 검토 (2026-08-23)

새 코드 넷은 전부 통과했다(결함 없음). 아래는 저장소 전체에 대한 지적 중 **남은 것**이다.
`signalsFrom` 통합은 지었다.

### F1. `select('*')` — 값은 페이로드가 아니라 **드리프트를 일찍 잡는 것**

sol 은 "페이로드 5–15% 감소" 로 값을 매겼지만, 이 저장소는 이미 다른 이유로 다쳤다.
`sync.ts:139` 의 주석이 그것을 기록한다 — `select('*')` 가 **`military_info` 를 잃은
운영 스키마를 조용히 받아들여** 로그인은 성공하고, 실패는 사용자가 복무 정보를 저장할
때까지 미뤄졌다. 컬럼을 적어 두면 그 순간 쿼리가 실패한다.

남은 자리: `events.ts:28` · `records.ts:256` · `tasks.ts:37` · `trips.ts:176,194,281` ·
`sync.ts:180,210`.

**짓기 전에 필요한 것:** 컬럼을 손으로 나열하다 하나 빠뜨리면 그 필드가 조용히
`undefined` 가 된다 -- 과다 페치보다 나쁘다. 각 함수의 row 매퍼가 읽는 컬럼과 `select`
목록이 일치하는지 **실행해서 확인하는 테스트**를 먼저 붙인다. 그 테스트 없이 하는
치환은 이 항목이 막으려는 결함을 그대로 만든다.

### F2. 정렬 안에서 `new Date()` — `RecordPage.tsx:371`

비교 함수 안에서 매번 Date 를 만든다. 하루 기록이 100건을 넘으면 재는 값이 나온다.
정렬 전에 키를 한 번 뽑으면 된다. **먼저 재고 고친다** -- 지금은 그 정도 기록을 가진
커플이 있는지 모른다.

### F3. 마운트되지 않는 위젯 트리 (~1,080줄)

`RoleHome` 480줄 + 위젯 생태계 ~600줄. 번들에는 안 들어간다(어느 라우트도 참조하지
않아 트리셰이킹된다). **지우지 않는다** -- `CURRENT_STATE.md` §4가 되돌릴 자리를
기록해 두고 있다. 되돌리지 않기로 정해지면 그때 지운다.

### F4. 번들 645 kB (gzip 193 kB)

Supabase 218 kB + React 50 kB + 앱 코드. sol 의 권고는 **운영에서 먼저 재라**는 것이고
그 판단이 옳다. 4G 에서 ~1.4초이며, 첫 페인트가 이미 괜찮다면 손댈 값이 아니다.

---

## E. 이 문서를 닫는 조건

각 항목이 지어지면 여기서 **지우고** [`V4_AS_BUILT.md`](V4_AS_BUILT.md)에 한 줄
들어간다. 이 문서에 남은 것이 곧 남은 일이다. 다 지워지면 파일을 지운다.
