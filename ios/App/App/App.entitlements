<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
		Data Protection.

		The whole app container is created with the Complete protection class, so
		diary text, attachments cached by WebKit and the Supabase session token
		in WKWebView localStorage are unreadable while the device is locked. This
		is the iOS counterpart of android:allowBackup="false" plus
		data_extraction_rules.xml: it closes the "someone has the phone" path
		rather than the "someone has the backup" path.

		The app does no background work (no background fetch, no background
		URLSession). Push notifications arrived with Gate 3 and do NOT change
		that: the payload is one fixed sentence rendered by the system, this app
		registers no handler that runs while the device is locked, and nothing in
		the notification path reads the container. A future notification that
		wanted to show anything about a record would need container access on a
		locked device, and this protection class is what would stop it.

		REQUIRES the "Data Protection" capability on the App ID in the Apple
		Developer portal (Complete Protection). Without it, signing fails with a
		provisioning-profile mismatch. That step is in
		docs/kiro/NATIVE_RELEASE_GUIDE.md. Simulator builds with
		CODE_SIGNING_ALLOWED=NO ignore entitlements entirely, so CI does not
		prove this; on-device behaviour is a DEVICE GATE.
	-->
	<key>com.apple.developer.default-data-protection</key>
	<string>NSFileProtectionComplete</string>

	<!--
		Sign in with Apple uses the first-party Capacitor bridge backed by
		AuthenticationServices, so the native App target declares the Default
		Sign in with Apple entitlement.

		The App ID capability is enabled separately in the Apple Developer
		portal. A signed development or distribution profile must include this
		entitlement before device or TestFlight activation; local metadata and
		unsigned builds do not prove profile matching or hosted login readiness.
	-->
	<key>com.apple.developer.applesignin</key>
	<array>
		<string>Default</string>
	</array>

	<!--
		DELIBERATELY ABSENT ENTITLEMENTS

		aps-environment
			Gate 3 ships the push CLIENT: permission, token registration, and the
			tap handler. The entitlement that lets APNs actually deliver is NOT
			added here, because it is inseparable from signing. It has to match
			a provisioning profile whose App ID has the Push Notifications
			capability enabled, and that is an Apple Developer portal action
			performed with real credentials. Adding the key without that profile
			makes device signing fail rather than making delivery work.
			Simulator builds ignore entitlements, so CI would not have caught the
			mismatch either. Add this together with the portal capability when
			TestFlight distribution is set up; see
			docs/kiro/NATIVE_RELEASE_GUIDE.md.

		keychain-access-groups
			The app DOES use the Keychain, and this key is still absent on
			purpose. Getting the reason right matters, because it is where the
			E2EE device keys live.

			Keychain use is confined to the first-party device-keys plugin:
			packages/capacitor-device-keys/ios/Sources/DeviceKeysPlugin/
			DeviceKeys.swift holds the P-256 device key (Secure Enclave via
			SecAccessControlCreateWithFlags, falling back to a software
			kSecClassKey item when the Enclave is unavailable), and LocalKeys.swift
			holds the local content key as a kSecClassGenericPassword item.
			src/lib/iosPrivacyManifest.test.ts measures that those two files are
			the ONLY Keychain callers in the compile surface, so the boundary
			cannot quietly widen.

			Neither call site sets kSecAttrAccessGroup, so both items land in the
			app's default access group, readable by this bundle alone. That is
			exactly the property that must not be given up. Declaring
			keychain-access-groups would place these items in a group any other
			bundle carrying the same entitlement could read, which for the device
			key means handing another process the material that authenticates this
			device. The entitlement is therefore not added, and the plugin must go
			on setting no access group. See the Keychain section of
			docs/kiro/NATIVE_RELEASE_GUIDE.md.

		com.apple.developer.associated-domains
			Deep linking uses the custom `gomsinlog` scheme, not Universal
			Links, so there is no domain to associate and no
			apple-app-site-association file to host.

		com.apple.developer.icloud-container-identifiers / ubiquity-*
			No iCloud storage. Sync is Supabase only.

	-->
</dict>
</plist>
